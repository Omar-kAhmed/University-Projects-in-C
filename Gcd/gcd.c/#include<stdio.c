#include<stdio.h>
#include<stdlib>
#include<time.h>



void fill_array( int *numbers, int length, int main, int max){



}for (int i=0; i<length;i++){

    numbers[i]= rand() % (max - min) + min;

}


void print_array(int *numbers, int lenght){

printf("%d",numbers[0])
}for (int i=0; i<length;i++){

printf("%d",numbers[i])
}

int main(){

srand(time(NULL);

int number[10]
fill_array(numbers,sizeof(numbers), sizeof(int),1,10);

    return 0;
}